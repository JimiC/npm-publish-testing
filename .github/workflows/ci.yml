name: Node CI

on: push

jobs:
  test:
    name: Test

    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os:
          - ubuntu-latest
        node_js:
          - 8

    steps:
      - name: NPM registry
        uses: actions/npm@v2.0.0        
        with:
          args: config list
